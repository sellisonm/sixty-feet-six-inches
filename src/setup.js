const card = require('./card')

let setupGame = () => {

    // 1. Load Cards From JSON data
    let cards = card.loadCardsFromData()
    console.log(cards)

    // 2. 

}

module.exports = {
    setupGame
}